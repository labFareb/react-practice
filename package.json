{
    "name": "react-projects-bepee",
    "version": "1.0.0",
    "scripts": {
        "build:counter": "npm --prefix counter run build",
        "build:tic-tac-toe": "npm --prefix tic-tac-toe run build"
    }
}